[Unit]
Description=Shutdown system if minecraft container is down
After=docker.service
Requires=docker.service

[Service]
Type=simple
ExecStart=/usr/local/bin/check_mc_container_down.sh
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
